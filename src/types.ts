export type ColumnType = {
  uid: string;
  firstName: string;
  lastName: string;
  age: number;
};

export interface Profile {
  uid: string;
  firstName: string;
  lastName: string;
  age: number;
}
